{% extends 'base.html.twig' %}

{% block body %}
    <nav class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="{{ path('history_index') }}">Прокат автомобилей</a>
            </div>
            <div id="navbar" class="collapse navbar-collapse">
                <ul class="nav navbar-nav">
                    <li class="active"><a href="{{ path('rentalpoint_index') }}">Пункты проката</a></li>
                    <li><a href="{{ path('history_index') }}">История</a></li>
                    <li><a href="{{ path('car_index') }}">Автомобили</a></li>

                </ul>
            </div><!--/.nav-collapse -->
        </div>
    </nav>

    <ul class="breadcrumb">
        <li><a href="{{ path('rentalpoint_index') }}">Список</a></li>
        <li class="active">Просмотр пункта проката</li>
    </ul>

    <h1>Запись пункта проката</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>№</th>
                <td>{{ rentalPoint.id }}</td>
            </tr>
            <tr>
                <th>Адрес:</th>
                <td>{{ rentalPoint.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary"  href="{{ path('rentalpoint_edit', { 'id': rentalPoint.id}) }}">Редактировать</a>

    {{ form_start(delete_form) }}
        <input class="btn btn-danger" type="submit" value="Удалить">
    {{ form_end(delete_form) }}

{% endblock %}
